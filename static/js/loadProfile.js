//attached to loadProfile.html
//not needed as button does POST request on its own

/*
fetch(window.location.pathname, {
    method: 'POST'
})
.then(response => { //this is how you actually redirect why man
    if(response.redirected)
    {
         window.location.href = window.location.pathname.split("/add").join("");//parses url to output just 
    }
})

*/